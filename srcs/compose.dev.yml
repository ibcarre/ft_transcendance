
services:
  postgres:
  nginx:
  backend:
    volumes:
      - ./backend/:/app
  
  frontend:
    build:
      context: ./frontend/
      dockerfile: ./Dockerfile.dev
    volumes:
      - ./frontend/:/app
